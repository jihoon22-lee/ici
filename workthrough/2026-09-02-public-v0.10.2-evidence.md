# Public v0.10.2 release evidence

## Overview

v0.10.2는 더 이상 release candidate가 아니다. 공개 [GitHub Release
v0.10.2](https://github.com/jihoon22-lee/ici/releases/tag/v0.10.2)는 exact `main` commit
`3b50dd4c485ddab212beb23ff820e82286a06e77`를 대상으로 게시됐고, provenance·artifact
검증과 공개 main report 배포가 모두 완료됐다. 이 문서는 현재 문서가 참조할 최종 release
근거를 한곳에 고정하고, 이전 candidate 및 release snapshot 문서는 historical evidence로
보존한다.

## Context

문서 갱신 시작 시점의 worktree는 clean `main`이었고 HEAD는
`3b50dd4c485ddab212beb23ff820e82286a06e77`였다. exact-main [CI run
33541134010](https://github.com/jihoon22-lee/ici/actions/runs/33541134010)이 성공한 뒤,
tag `v0.10.2`가 같은 commit을 가리키고 [release run
33541928666](https://github.com/jihoon22-lee/ici/actions/runs/33541928666)이 완료된
상태를 읽기 전용 API로 확인했다. 이 작업은 문서만 갱신하며 GitHub tag, release, PR, merge,
push는 수행하지 않았다.

## Changes Made

### 1. Current release status

- README, 사용자 가이드, CI/CD 연동 가이드의 current release 설명을 v0.10.2 public
  release로 갱신했다.
- exact-main commit, `Merge Gate`, `Publish Main Verification Report`, release run,
  non-draft/non-prerelease 상태, 정확히 9개 asset, `ici.pyz` checksum을 명시했다.
- main ici/viewer Pages의 HTTP/content-type/title/Zero-CDN 독립 audit 결과와 이 문서 링크를
  사용자 문서에 연결했다.

### 2. Plan and handover state

- 마스터 계획의 2026-09-02 current 기준을 추가하고 2026-09-01 내용은 snapshot으로
  명시했다.
- handover의 current 기준을 exact main과 v0.10.2로 갱신하고, v0.9.1 이하 release
  evidence는 historical snapshot으로 남겼다.
- I4-2의 남은 작업을 release artifact 확정이 아니라 released ici를 사용하는
  BuildScope B5 교차 검증으로 정정했다. I4-3/I4-4와 I4 전체 checkpoint는 별도 완료 조건으로
  유지한다.

### 3. CHANGELOG synchronization

`CHANGELOG.md`의 v0.10.2 항목에 public release URL, exact tag target, CI/release run,
9개 asset, pyz checksum, main Pages audit를 추가했다.

## Release and CI evidence

### Exact-main CI run

Run `33541134010` on commit `3b50dd4c485ddab212beb23ff820e82286a06e77` completed with
conclusion `success`.

| job | conclusion |
|---|---|
| `Verify & Dogfood ici` | `success` |
| `Viewer GUI build (Qt5)` | `success` |
| `Viewer GUI build (Qt6)` | `success` |
| `Publish Main Verification Report` | `success` |
| `Merge Gate` | `success` |
| `Publish PR Report & Sticky Comment` | `skipped` (expected for `push` to `main`) |

### Release workflow

Run `33541928666` completed with conclusion `success` on the exact same commit.

| job | conclusion |
|---|---|
| `Validate Release Provenance` | `success` |
| `Build & Publish Release` | `success` |

The release is `non-draft` and `non-prerelease`. Its `v0.10.2` tag dereferences to
`3b50dd4c485ddab212beb23ff820e82286a06e77`.

## Release asset audit

The public release contains exactly these nine assets. Values below are the GitHub Release API
size and SHA-256 digest; the published `ici.pyz.sha256` manifest reports the same digest as the
`ici.pyz` asset.

| asset | bytes | SHA-256 |
|---|---:|---|
| `ici.pyz` | 2208714 | `8e6237302ff3b6198cad86c97dd6bcd666ecab9204e9e19209e2e310c7fd18f4` |
| `ici.pyz.sha256` | 74 | `ee0f3db4afc0b5d9008bdee139ac1348db7f6e9bc54d5adac0a981156ac0d0d0` |
| `ici-self-report.html` | 6772382 | `86267a69297458d04d781e4871a425cc6026737c0ed671de908a456c35d5a494` |
| `ici-self-report.json` | 16095002 | `efed10fc4882c7026549620ab338cd236ddcdcd10f67efa06194a7e6c408d9d8` |
| `viewer-report.html` | 356367 | `4463355c090654f1d7ef89435a0e500bdd601a2b45e89bbbe2e035c488d37444` |
| `viewer-report.json` | 699150 | `21c97261409f8512f31f74b34a4580e585a709fd82bf32e327146e82defa1ff5` |
| `icirv` | 2573000 | `56a2c419daba10027d6ca3d2870d0f568283641cf69b1b2e9ca30950e4decbe8` |
| `icirv-gui` | 268672 | `d1cd848df70220881a85f8ffc49eabe094b7e713798980ce29dc101321ec2a01` |
| `icirv-gui.README.txt` | 558 | `ec3fb3b2bf3545b9c296e685cb1c7c106e8af6b77e4266159a83b0342811d16e` |

## Independent main Pages audit

The two published main report pages were fetched independently after publication with a cache-
busting query. Both returned HTTP 200 and `text/html; charset=utf-8`; the exact titles and
external resource URL count were:

| page | title | external resource URLs |
|---|---|---:|
| [ici/main](https://jihoon22-lee.github.io/ici/ici/main/) | `ici Verification Report — ici` | 0 |
| [viewer/main](https://jihoon22-lee.github.io/ici/viewer/main/) | `ici Verification Report — viewer` | 0 |

The resource scan covered executable/display tags (`script`, `link`, `img`, `iframe`, audio/video
and related tags) and CSS `url(...)` references. Navigation links in the report are not treated as
resource dependencies.

## Verification Results

### Documentation checks

```text
git status --short --branch       clean before branch creation
git rev-parse HEAD                3b50dd4c485ddab212beb23ff820e82286a06e77
release asset count               9
release isDraft/isPrerelease      false/false
tag target                        3b50dd4c485ddab212beb23ff820e82286a06e77
ici.pyz checksum                  8e6237302ff3b6198cad86c97dd6bcd666ecab9204e9e19209e2e310c7fd18f4
```

After editing, Markdown links, stale candidate/current phrases, and the diff were checked with
`rg`, `git diff --check`, and targeted link/path inspection. No historical workthrough or release
evidence file was deleted.

## Next Steps

Use the public v0.10.2 artifact for the remaining toy-projects BuildScope B5 cross-repository
verification. Keep I4-3/I4-4 and the full I4 checkpoint separate from this corrective release
boundary.
