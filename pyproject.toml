[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ici"
description = "Integrated CI — 폐쇄망(RHEL 8/tcsh)과 개발환경(WSL)에서 단일 파일로 동작하는 CI/CD 검증·빌드 엔진"
readme = "README.md"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = [
    # 전부 py3-none-any 순수 파이썬 휠이어야 한다 (build-pyz.sh 가 검사한다).
    "typer>=0.12",
    "rich>=13.7",
    "tomli>=2.0",
    "tomli-w>=1.0",
]

[project.scripts]
ici = "ici.__main__:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "shiv>=1.0.6",
    "mypy>=1.10",
    "coverage>=7.0",
    # lint 엔진은 PATH 또는 프로젝트 venv 에서 ruff 를 찾는다(engines/lint.py:_find_ruff_command).
    # dev 의존성으로 선언해야 CI 의 .venv 에 들어가고, 엔진이 AST 폴백으로 내려가지 않는다.
    # 상한을 두는 이유: ruff format 규칙이 마이너 버전에서 바뀌면 --check 가 갑자기 깨진다.
    "ruff>=0.16,<0.17",
]

[tool.hatch.version]
path = "src/ici/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/ici"]

[tool.ruff]
target-version = "py310"          # 3.11+ 문법을 lint 실패로 만든다
line-length = 100
src = ["src", "tests"]

[tool.ruff.format]
# Markdown prose is not source. ruff formats fenced Python blocks inside .md,
# and design docs quote class methods in context — reformatting strips their
# indentation and turns a method into a module-level function, so the document
# ends up describing something that would not compile.
exclude = ["*.md"]

[tool.ruff.lint]
select = [
    "E", "F", "W",   # pycodestyle / pyflakes
    "I",             # isort
    "UP",            # pyupgrade (target-version 이 py310 이라 3.10 이하로만 제안)
    "B",             # bugbear
    "SIM",           # simplify
    "TID",           # tidy-imports (banned-api)
    "RUF",
]
ignore = ["E501"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"tomllib".msg = "tomllib 은 3.11+ 다. 폐쇄망은 3.10 이므로 tomli 를 쓴다."
"requests".msg = "certifi 번들 CA 때문에 사내 TLS 인터셉션 환경에서 깨진다. stdlib urllib 을 쓴다."
"httpx".msg = "certifi 번들 CA 때문에 사내 TLS 인터셉션 환경에서 깨진다. stdlib urllib 을 쓴다."
"certifi".msg = "시스템 CA 를 써야 한다. ssl.create_default_context()."

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
